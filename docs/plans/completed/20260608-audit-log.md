# Аудит-лог действий пользователей (issue #23)

## Overview

Добавить сквозной журнал аудита значимых действий пользователей, покрывающий все слои монорепо:

- **API** — автоматическая фиксация действий через декларативный декоратор `@Audit(...)` + глобальный `AuditInterceptor`, без ручных вызовов в бизнес-логике.
- **Хранение** — отдельная таблица `audit_logs` в Prisma с индексами под типовые фильтры.
- **Типы** — Zod-схемы события аудита в `@repo/types` как единый source of truth для API и web.
- **Web** — страница `/admin/audit` с серверной фильтрацией (актор / тип события / диапазон дат), сортировкой по дате и пагинацией, по образцу существующей таблицы пользователей.

**Проблема, которую решает:** сейчас невозможно узнать, кто и когда выполнял значимые действия (логин, смена роли и т.п.). Для production-ready основы такой журнал нужен для безопасности и разбора инцидентов.

**Интеграция:** новый модуль `audit` встраивается рядом с `auth`/`users`, переиспользует слой репозитория (ADR-007), RBAC-гарды (`JwtAuthGuard` + `RolesGuard`), паттерн API-клиента фронта (ADR-008) и паттерн серверной таблицы из `admin/users`.

## Context (from discovery)

Файлы/компоненты, на которые опирается реализация:

- **Prisma:** `apps/api/prisma/schema.prisma` — enum `Role`, модель `User`; индексы добавляются через `@@index`.
- **Auth:** `apps/api/src/auth/auth.controller.ts` (login/logout), `auth.service.ts` (`validateUser` бросает `UnauthorizedException` при неуспешном логине).
- **Users:** `apps/api/src/users/users.controller.ts` (`updateRole`), слой репозитория `users.repository.ts` (паттерн `findAllPublic` с фильтрами/сортировкой, `BaseRepository`).
- **RBAC:** `apps/api/src/auth/guards/{jwt-auth,roles}.guard.ts`, `decorators/{roles,current-user}.decorator.ts`.
- **Регистрация модулей:** `apps/api/src/app.module.ts`.
- **Типы:** `packages/types/src/auth.ts` + `index.ts` (re-export). Zod как source of truth.
- **Web:** `apps/web/src/app/admin/users/{page,users-table,role-select}.tsx`, `hooks/use-users.ts`, `api/users.api.ts`, `lib/api.ts`; защита роли — `apps/web/src/app/admin/layout.tsx` (redirect не-ADMIN), навигация в шапке layout.
- **Тесты:** Jest + ts-jest, файлы `*.spec.ts` рядом с кодом. Пример `health.controller.spec.ts` — юнит-тест с прямым `new Controller()` и моками зависимостей (без `TestingModule`).
- **Docs:** `docs/adr/` + индекс `docs/adr/README.md` (следующий номер — 010), `docs/guides/`, список фич в `README.md`.

Найденные паттерны:

- Репозиторий наследует `BaseRepository<...>` и кастует prisma-делегат; публичная проекция через `... satisfies Prisma.XSelect`.
- DTO query — `class-validator` (`@IsOptional`, `@IsEnum`, `@IsIn`), серверная фильтрация/сортировка в репозитории.
- Web-таблица: TanStack Table в `manualSorting`/`manualFiltering`, дебаунс текстового фильтра, `params` в `queryKey` react-query.

Зависимости: дополнительных npm-пакетов не требуется (механизм на штатных NestJS interceptor + `Reflector`).

## Development Approach

- **testing approach**: Regular (сначала реализация, затем тесты в том же таске).
- complete each task fully before moving to the next.
- make small, focused changes.
- **каждый таск с кодовой логикой обязан содержать новые/обновлённые тесты** (success + error/edge кейсы); тесты — отдельные пункты чек-листа, не объединять с реализацией.
- **все тесты должны проходить перед началом следующего таска**.
- **обновлять этот файл плана при изменении скоупа** (➕ новые задачи, ⚠️ блокеры).
- запускать тесты после каждого изменения; сохранять обратную совместимость.

## Testing Strategy

- **unit-тесты (Jest, `*.spec.ts`)**: обязательны для модуля аудита — `AuditService`, `AuditRepository` (фильтры/сортировка/пагинация с моком Prisma), `AuditInterceptor` (резолв метаданных, success/error-ветки, не-логирование чувствительных данных), `@Audit`-декоратор (метаданные через `Reflector`).
- **тесты Zod-схем — в API-пакете** (решено): в `@repo/types` нет тест-раннера, и заводить там Jest ради одного файла — лишнее (YAGNI). API уже импортирует `@repo/types` и имеет Jest, поэтому проверку парсинга схем аудита размещаем в `apps/api` (например, `apps/api/src/audit/audit.schemas.spec.ts`). Новый toolchain в `@repo/types` НЕ добавляем.
- **e2e на `GET /audit`** (определено, не опционально): проект имеет `apps/api/test/app.e2e-spec.ts` (Fastify). Гард-поведение — ключевая гарантия безопасности, поэтому добавляем e2e «403 для USER, 200 для ADMIN» в Task 6.
- **web без unit-тестов** (решено): ни `apps/web`, ни UI-e2e раннера в проекте нет. Web-таски (7, 8) **исключены из правила обязательных unit-тестов** — покрываются `typecheck` + `lint` и ручной проверкой (см. Post-Completion). Это зафиксировано осознанно, чтобы не подразумевать ненаписанные тесты.

## Progress Tracking

- помечать выполненные пункты `[x]` сразу по факту.
- ➕ — новые обнаруженные задачи, ⚠️ — блокеры.
- держать план в синхронизации с фактической работой.

## Solution Overview

**Механизм (выбран):** декларативный декоратор `@Audit(options)` на методах контроллеров + один глобальный `AuditInterceptor`, читающий метаданные через `Reflector`.

Поток:

```
HTTP-запрос
  -> Guard (JwtAuthGuard) проставляет req.user
  -> AuditInterceptor: читает @Audit-метаданные хендлера
       success -> tap(): пишет событие options.event (success=true)
       error   -> catchError(): пишет options.failureEvent ?? options.event (success=false), затем rethrow
  -> AuditService.record() -> AuditRepository.create()  (fire-and-forget, ошибки аудита не ломают запрос)
```

Ключевые решения и обоснование:

1. **Декоратор + interceptor**, а не event-based: фиксация остаётся декларативной и рядом с эндпоинтом, бизнес-логика не засоряется ручными `emit`-вызовами (требование issue), неуспешный логин ловится через `catchError` без явного кода в сервисе.
2. **Whitelist метаданных**: interceptor НИКОГДА не логирует тело запроса целиком. В лог попадают только поля, явно возвращённые резолверами `target`/`metadata`/`actor` из декоратора. Это гарантирует, что пароли/токены не утекут в журнал.
3. **`actorId` без FK-связи на `User`**: храним `actorId`/`targetId` как обычные строки + денормализованный `actorEmail`. Журнал должен переживать удаление пользователя, поэтому каскадных связей и `onDelete` нет.
4. **`metadata` как `Json`**: структура события расширяема без миграций — легко добавлять новые типы событий (требование issue).
5. **Доступ к журналу — только ADMIN** (`JwtAuthGuard` + `RolesGuard` + `@Roles(ADMIN)` на API; redirect не-ADMIN в `admin/layout.tsx` на web).

## Technical Details

### Prisma — модель и enum

```prisma
enum AuditEvent {
  LOGIN_SUCCESS
  LOGIN_FAILED
  LOGOUT
  USER_ROLE_CHANGED
}

model AuditLog {
  id         String     @id @default(cuid())
  event      AuditEvent
  success    Boolean    @default(true)
  actorId    String?    // id актора; null для неуспешного логина. Без FK — лог переживает удаление user
  actorEmail String?    // денормализованный email актора для отображения без join
  targetId   String?    // id целевой сущности (напр. пользователь, чью роль меняют)
  targetType String?    // тип целевой сущности ("User")
  metadata   Json?      // структурированные доп. данные (только whitelisted поля)
  ip         String?
  userAgent  String?
  createdAt  DateTime   @default(now())

  @@index([createdAt])
  @@index([event])
  @@index([actorId])
  @@map("audit_logs")
}
```

### `@repo/types` — схемы (новый файл `audit.ts`)

- `AuditEventSchema = z.enum([...])`, `AuditEvent`.
- `AuditLogSchema` (id, event, success, actorId/Email, targetId/Type, metadata, ip, userAgent, createdAt: `z.coerce.date()`).
- `ListAuditQuerySchema` / `ListAuditParams`: `actor?`, `event?`, `dateFrom?`, `dateTo?`, `sortOrder?` (`asc`/`desc` по `createdAt`), `limit?`, `offset?`.
- `AuditPageSchema` — ответ `{ items: AuditLog[]; total: number }`.
- `AUDIT_SORTABLE_FIELDS = ['createdAt'] as const` (единый источник истины).

### API — декоратор и опции

⚠️ Типизация request: в проекте нет готового augmented-типа `FastifyRequest` с `user`. Passport (`jwt.strategy.validate()`) кладёт в `req.user` **полный Prisma-`User`** (не slim `{id,email}`). Под strict mode голый `FastifyRequest` не даёт доступа к `user`/`body`/`params`, поэтому в декораторе/interceptor вводим локальный тип:

```ts
import type { User } from '@prisma/client';
import type { FastifyRequest } from 'fastify';

// Локальный тип запроса для резолверов аудита.
type AuditRequest = FastifyRequest & {
  user?: User; // полный User от JwtAuthGuard (Passport)
  body?: Record<string, unknown>; // тело запроса (резолверы берут только whitelisted поля)
  params?: Record<string, string>; // route-параметры
};

export interface AuditOptions {
  event: AuditEvent; // событие при успехе
  failureEvent?: AuditEvent; // событие при ошибке (напр. LOGIN_FAILED)
  targetType?: string; // "User"
  target?: (req: AuditRequest) => string | undefined; // id целевой сущности
  actor?: (req: AuditRequest) => { id?: string; email?: string } | undefined; // переопределение актора
  metadata?: (req: AuditRequest) => Record<string, unknown> | undefined; // только безопасные поля
}
export const AUDIT_KEY = 'audit:options';
export const Audit = (options: AuditOptions) => SetMetadata(AUDIT_KEY, options);
```

Дефолт актора в interceptor: `req.user` (читаем `user.id`/`user.email` из полного `User`), проставленный `JwtAuthGuard`. Для login (без guard) `req.user` отсутствует — актор берётся из `options.actor` → `req.body.email`. IP — `req.ip`, UA — `req.headers['user-agent']`.

### API — разметка декораторами

- `POST /auth/login`: `@Audit({ event: LOGIN_SUCCESS, failureEvent: LOGIN_FAILED, actor: (r) => ({ email: r.body?.email }), metadata: (r) => ({ email: r.body?.email }) })` — пароль не логируется.
- `POST /auth/logout`: `@Audit({ event: LOGOUT })` (актор из `req.user`).
- `PATCH /users/:id/role`: `@Audit({ event: USER_ROLE_CHANGED, targetType: 'User', target: (r) => r.params.id, metadata: (r) => ({ role: r.body?.role }) })`.

### API — репозиторий (контракт BaseRepository)

⚠️ Важно: `BaseRepository` (`apps/api/src/common/repository/base.repository.ts`) даёт только CRUD-по-id через структурный `BaseModelDelegate` — у него **нет** `count` и `$transaction`, а `create` объявлен `protected`. Нетривиальные запросы делаются как в `UsersRepository`: инъекция `PrismaService` напрямую и обращение к типизированному делегату `this.prisma.auditLog.*`.

Поэтому `AuditRepository`:

- `extends BaseRepository<AuditLog, Prisma.AuditLogCreateInput, Prisma.AuditLogUpdateInput>` с кастом делегата `prisma.auditLog as unknown as BaseModelDelegate<...>` (как в `users.repository.ts`);
- `record()` — публичный метод-обёртка: `override record`/`create` поверх `super.create(...)`, чтобы вызывать из `AuditService` (по образцу `override create` в `UsersRepository`);
- `findPage()` — НЕ через базовый делегат: `this.prisma.$transaction([this.prisma.auditLog.findMany({...}), this.prisma.auditLog.count({ where })])`.

`findPage` — `where`: `event` (eq), `actor` (по `actorEmail contains insensitive` ИЛИ `actorId eq`), `createdAt` (`gte dateFrom`, `lte dateTo`). `orderBy: { createdAt: sortOrder ?? 'desc' }`. `take: limit ?? 50` (cap 100), `skip: offset ?? 0`. Возврат `{ items, total }`.

### Web

- `audit.api.ts`: `ListAuditParams`, `auditApi.list(accessToken, params)` → `GET /audit`.
- `use-audit.ts`: `useAuditLogs(params)` (react-query, `params` в `queryKey`, `enabled` по accessToken).
- `admin/audit/page.tsx` + `audit-table.tsx`: TanStack Table (`manualSorting`/`manualFiltering`), фильтры — input по актору (с дебаунсом), select по типу события, два date-input (от/до), сортировка по дате, простая пагинация (prev/next по `offset`).
- Навигация: добавить ссылку «Аудит» в шапку `admin/layout.tsx`.

## What Goes Where

- **Implementation Steps** (`[ ]`): Prisma-модель/миграция, типы, модуль аудита, декоратор+interceptor, разметка эндпоинтов, list-эндпоинт, web-страница, документация — всё внутри репозитория.
- **Post-Completion** (без чекбоксов): ручная проверка web-журнала, нагрузочная проверка роста таблицы, политика ретеншна/ротации (вынесена за рамки задачи — документируется как развитие).

## Implementation Steps

### Task 1: Prisma — модель AuditLog, enum и миграция

**Files:**

- Modify: `apps/api/prisma/schema.prisma`
- Create: миграция в `apps/api/prisma/migrations/` (генерируется `db:migrate`)

- [x] добавить enum `AuditEvent` и модель `AuditLog` с индексами `@@index([createdAt])`, `@@index([event])`, `@@index([actorId])` и `@@map("audit_logs")`
- [x] `actorId`/`targetId` — обычные `String?` без relation (журнал переживает удаление user); `metadata Json?`
- [x] зафиксировать комментарием неизбежное дублирование значений enum: по CLAUDE.md source of truth — Zod-схема `AuditEventSchema` в `@repo/types`, но Prisma-enum не может импортировать из неё, поэтому Prisma зеркалит Zod (значения сверяются вручную, ведёт `@repo/types`)
- [x] сгенерировать миграцию: применена `20260608154611_add_audit_log`
- [x] обновить Prisma Client: `pnpm --filter @repo/api db:generate`
- [x] проверка типов: `pnpm --filter @repo/api typecheck` (build недоступен — `dist/` принадлежит root из docker-контейнера; типы Prisma подхватились). Юнит-логики в таске нет — поведение покрывается тестами репозитория в Task 4
- [x] ⚠️➕ исправлен `DATABASE_URL` в `.env`: порт 5432→5441 (на 5432 оказалась БД чужого проекта; docker-postgres этого проекта проброшен на 5441)

### Task 2: `@repo/types` — схемы события аудита

**Files:**

- Create: `packages/types/src/audit.ts`
- Modify: `packages/types/src/index.ts`
- Create: `apps/api/src/audit/audit.schemas.spec.ts` (тесты схем — в API, где уже есть Jest; в `@repo/types` тест-раннер НЕ заводим)

- [x] описать `AuditEventSchema`, `AuditLogSchema`, `ListAuditQuerySchema`, `AuditPageSchema`, `AUDIT_SORTABLE_FIELDS` и их типы
- [x] ре-экспортировать из `index.ts`
- [x] проверить совпадение значений `AuditEvent` с Prisma-enum (ручная сверка; Zod ведёт — см. заметку в Task 1)
- [x] тесты (в `apps/api`): парсинг валидного `AuditLog`, отклонение неизвестного `event`, coerce `createdAt` (success + error)
- [x] запустить тесты — 6/6 проходят
- [x] ⚠️➕ пакет `@repo/types` пересобран внутри docker-контейнера: `dist/` принадлежит root (создан контейнерами), на хосте пересборка невозможна (EACCES). Кросс-пакетные импорты резолвятся через `dist`, поэтому после правок в `@repo/types` нужна пересборка через `docker compose exec api pnpm --filter @repo/types build`

### Task 3: API — каркас модуля audit (repository + service)

**Files:**

- Create: `apps/api/src/audit/audit.module.ts`
- Create: `apps/api/src/audit/audit.repository.ts`
- Create: `apps/api/src/audit/audit.service.ts`
- Create: `apps/api/src/audit/audit.repository.spec.ts`
- Create: `apps/api/src/audit/audit.service.spec.ts`
- Modify: `apps/api/src/app.module.ts`

- [x] `AuditRepository extends BaseRepository<...>` строго по образцу `UsersRepository`: инъекция `PrismaService`, каст делегата `prisma.auditLog as unknown as BaseModelDelegate<...>`
- [x] `record(data)` — публичный метод поверх `super.create(...)` (т.к. базовый `create` — `protected`)
- [x] `findPage(query)` — НЕ через базовый делегат: `this.prisma.$transaction([auditLog.findMany({...}), auditLog.count({ where })])`, фильтры actor/event/диапазон дат, сортировка по `createdAt`, пагинация `take/skip` (cap 100), возврат `{ items, total }`
- [x] `AuditService` с `record(entry)` (fire-and-forget, ошибка логируется и не пробрасывается) и `findPage(query)`
- [x] `AuditModule` (providers: repository, service; exports: service) и импорт в `AppModule`
- [x] тесты репозитория: формирование `where` для каждого фильтра, дефолтные `sortOrder=desc`/`limit`/cap, пагинация (мок `PrismaService`)
- [x] тесты сервиса: проброс в репозиторий (record/findPage), success + edge (ошибка record проглатывается)
- [x] запустить тесты — 16/16 проходят

### Task 4: API — декоратор `@Audit` и `AuditInterceptor`

**Files:**

- Create: `apps/api/src/audit/decorators/audit.decorator.ts`
- Create: `apps/api/src/audit/audit.interceptor.ts`
- Create: `apps/api/src/audit/audit.interceptor.spec.ts`
- Modify: `apps/api/src/audit/audit.module.ts` (регистрация `APP_INTERCEPTOR`)

- [x] `@Audit(options)` через `SetMetadata(AUDIT_KEY, options)` + интерфейс `AuditOptions`
- [x] `AuditInterceptor`: читает опции через `Reflector` (нет опций → пропуск); извлекает actor (`req.user` или `options.actor`), `ip`, `userAgent`; `target`/`metadata` через резолверы
- [x] success-ветка (`tap`) пишет `event` с `success=true`; error-ветка (`catchError`) пишет `failureEvent ?? event` с `success=false` и пробрасывает ошибку дальше
- [x] запись fire-and-forget вынесена в `AuditService.record` (ошибка логируется и НЕ ломает запрос)
- [x] зарегистрировать глобально через `{ provide: APP_INTERCEPTOR, useClass: AuditInterceptor }` в `AuditModule`
- [x] тесты interceptor: пропуск без декоратора; success пишет нужное событие; error пишет failureEvent и rethrow'ит; метаданные только whitelisted (нет `password`); target/targetType
- [x] запустить тесты — 21/21 проходят
- [x] ➕ `AuditRequest` — минимальный структурный тип (без зависимости `fastify`; в проекте нет augmented `FastifyRequest`)

### Task 5: API — разметка эндпоинтов декоратором `@Audit`

**Files:**

- Modify: `apps/api/src/auth/auth.controller.ts`
- Modify: `apps/api/src/users/users.controller.ts`
- Modify/Create: `apps/api/src/auth/auth.controller.spec.ts`, `apps/api/src/users/users.controller.spec.ts`

- [x] login: `@Audit({ event: LOGIN_SUCCESS, failureEvent: LOGIN_FAILED, actor/metadata по email })` — пароль не попадает в metadata
- [x] logout: `@Audit({ event: LOGOUT })`
- [x] updateRole: `@Audit({ event: USER_ROLE_CHANGED, targetType: 'User', target по :id, metadata по role })`
- [x] тесты: метаданные `@Audit` считываются `Reflector`'ом для каждого хендлера; резолвер login-metadata не возвращает password
- [x] тест (ключевой error-prone случай): login-actor резолвит email из `req.body.email` (без `req.user`)
- [x] запустить тесты — 6/6 проходят

### Task 6: API — эндпоинт `GET /audit` (ADMIN)

**Files:**

- Create: `apps/api/src/audit/dto/list-audit-query.dto.ts`
- Create: `apps/api/src/audit/dto/audit-response.dto.ts`
- Create: `apps/api/src/audit/audit.controller.ts`
- Create: `apps/api/src/audit/audit.controller.spec.ts`
- Modify: `apps/api/src/audit/audit.module.ts`
- Modify: `apps/api/test/app.e2e-spec.ts` (e2e на доступ к `GET /audit`)

- [x] `ListAuditQueryDto` (class-validator: `actor?`, `event?` `@IsEnum`, `dateFrom?`/`dateTo?` `@Type(Date)`+`@IsDate`, `sortOrder?` `@IsIn`, `limit?`/`offset?` `@IsInt`/`@Min`/`@Max` + `@Type`) и `AuditResponseDto`/`AuditPageDto` для Swagger
- [x] `AuditController`: `GET /audit` с `@UseGuards(JwtAuthGuard, RolesGuard)` + `@Roles(ADMIN)` + Swagger-аннотации, делегирует в `AuditService.findPage`
- [x] зарегистрировать controller + `RolesGuard` в `AuditModule`
- [x] тесты контроллера: делегирование query в сервис
- [x] e2e: `GET /audit` → 403 для USER, 200 для ADMIN (реальный `RolesGuard`, замоканный `JwtAuthGuard` — без БД/env)
- [x] запустить тесты — unit 28/28 + e2e 2/2 проходят

### Task 7: Web — API-клиент и хук журнала

**Files:**

- Create: `apps/web/src/api/audit.api.ts`
- Create: `apps/web/src/hooks/use-audit.ts`

- [x] `audit.api.ts`: `ListAuditParams`, `AUDIT_SORTABLE_FIELDS`, `auditApi.list(accessToken, params)` → `GET /audit` (по образцу `users.api.ts`)
- [x] `use-audit.ts`: `useAuditLogs(params)` (react-query, `params` в `queryKey`, `enabled` по `session.accessToken`)
- [x] unit-тестов нет (в `apps/web` нет тест-раннера — решение зафиксировано в Testing Strategy); покрытие — typecheck/lint + ручная проверка (Post-Completion)
- [x] typecheck: `pnpm --filter @repo/web typecheck` — чисто

### Task 8: Web — страница и таблица журнала

**Files:**

- Create: `apps/web/src/app/admin/audit/page.tsx`
- Create: `apps/web/src/app/admin/audit/audit-table.tsx`
- Modify: `apps/web/src/app/admin/layout.tsx` (ссылка «Аудит» в навигации)

- [x] `page.tsx` (server component) по образцу `admin/users/page.tsx`; защита роли уже обеспечена `admin/layout.tsx`
- [x] `audit-table.tsx`: TanStack Table (`manualSorting`/`manualFiltering`), колонки дата/event/actor/target/результат; фильтры — input актора (дебаунс), select события, date-input от/до; сортировка по дате; пагинация prev/next (offset); состояния loading/error/empty
- [x] добавить ссылку «Аудит» (`/admin/audit`) в шапку `admin/layout.tsx`
- [x] unit-тестов нет (в `apps/web` нет тест-раннера); поведение фильтров/таблицы — ручная проверка (Post-Completion)
- [x] typecheck + lint: оба чисто

### Task 9: Документация

**Files:**

- Create: `docs/adr/010-audit-log.md`
- Modify: `docs/adr/README.md` (строка в индексе)
- Create: `docs/guides/adding-audit-event.md`
- Modify: `README.md` (список фич)

- [x] ADR-010: решение «аудит через декоратор `@Audit` + глобальный interceptor», альтернативы (event-based, чистый interceptor), последствия; зафиксированы whitelist-метаданных, отсутствие FK на user и вынос ретеншна за рамки
- [x] добавить строку ADR-010 в индекс `docs/adr/README.md` (дата 2026-06-08)
- [x] гайд `adding-audit-event.md`: как добавить новый тип события (enum Prisma + миграция → `@repo/types` → `@Audit` на эндпоинте)
- [x] обновить список фич в `README.md` (добавить аудит-лог)
- [x] документация ревью — без кодовых тестов (правка доков)

### Task 10: Verify acceptance criteria

- [x] события LOGIN_SUCCESS/LOGIN_FAILED/LOGOUT/USER_ROLE_CHANGED попадают в журнал автоматически (@Audit + interceptor)
- [x] запись содержит актора, тип события, целевую сущность, метаданные, IP/UA, timestamp (модель AuditLog)
- [x] журнал доступен на web с фильтрацией по актору/типу/диапазону дат и сортировкой по дате
- [x] доступ к `GET /audit` и странице ограничен ролью ADMIN (guards + e2e 403/200)
- [x] миграция Prisma с индексами под фильтрацию применяется (`20260608154611_add_audit_log`)
- [x] пароль/токены не попадают в metadata (whitelist-резолверы + тесты interceptor/контроллера)
- [x] прогон: lint (api/web) ✓, typecheck (types/api/web) ✓, test (api 28 unit + 2 e2e) ✓, build (types/api/web — в контейнерах) ✓
- [x] ⚠️ build/lint/test через корневой turbo на хосте недоступны (turbo пересобирает `@repo/types`, чей `dist/` принадлежит root); проверки выполнены по пакетам напрямую и в docker-контейнерах

### Task 11: [Final] Завершение

- [x] обновить CLAUDE.md: добавлен модуль `audit` и заметка про `@Audit` + interceptor
- [x] переместить план в `docs/plans/completed/`

## Post-Completion

_Items requiring manual intervention or external systems — informational only_

**Ручная проверка:**

- пройти сценарии на web: успешный/неуспешный логин, логаут, смена роли → записи появились в `/admin/audit`; фильтры по актору/событию/датам и сортировка работают; пагинация листает.
- проверить, что не-ADMIN не видит пункт «Аудит» и получает redirect/403.
- убедиться, что в записях LOGIN\_\* нет паролей в `metadata`.

**Развитие (вне рамок задачи):**

- **Политика ретеншна/ротации**: при росте `audit_logs` добавить плановую очистку старых записей (cron/scheduled job) или партиционирование; вынести срок хранения в env (`AUDIT_RETENTION_DAYS`).
- **Обогащение `actorId` для LOGIN_SUCCESS**: сейчас для логина гарантирован `actorEmail`, `actorId` может быть null (актор резолвится из тела запроса до аутентификации). При необходимости — дорезолвить id по email при записи.
- web e2e (Playwright) на сценарии журнала, если в проект будет добавлен UI-e2e раннер.
